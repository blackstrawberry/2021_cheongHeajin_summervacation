<?php
    // 숫자에서 문자열로 자동 변환
    $number = 12345 * 67890;
    echo substr($number, 3, 3);

    // 문자열에서 숫자로의 자동 변환
    $pi = "3.1415927";
    $radius = 5;
    echo "<br><br>";
    echo $pi * ($radius * $radius);

?>
